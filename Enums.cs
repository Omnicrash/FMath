namespace Frostfire.Math
{
    public enum ContainmentType
    {
        Disjoint,
        Contains,
        Intersects,
    }

    public enum PlaneIntersectionType
    {
        Back,
        Front,
        Intersecting,
    }

    public enum Faces
    {
        None = 0,
        Front = 1,
        Left = 2,
        Back = 3,
        Right = 4,
        Top = 5,
        Bottom = 6
    }

}
